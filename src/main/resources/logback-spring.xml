<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="service" source="spring.application.name"/>
    <springProperty scope="context" name="kafkaBootstrap" source="spring.kafka.bootstrap-servers" defaultValue="localhost:9092"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd' 'HH:mm:ss.SSS} [%-12.12thread] %highlight(%-5level) %-27.27logger{1} [%X{traceId}] [%X{schemaId}] - %msg%n
            </pattern>
        </encoder>
    </appender>

    <appender name="KAFKA" class="com.flexi.common.logging.KafkaLogAppender">
        <topic>system.logs</topic>
        <bootstrapServers>${kafkaBootstrap}</bootstrapServers>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeTags>false</includeTags>
            <fieldNames>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <appender name="ASYNC_KAFKA" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="KAFKA"/>
        <queueSize>2048</queueSize>
        <neverBlock>true</neverBlock>
        <includeCallerData>false</includeCallerData>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_KAFKA"/>
    </root>

</configuration>